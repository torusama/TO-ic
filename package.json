{
  "name": "toic-learning",
  "version": "1.0.0",
  "description": "TOEIC Learning Platform",
  "main": "index.html",
  "scripts": {
    "predev": "node build-config.js",
    "dev": "vite --host 0.0.0.0",
    "prebuild": "node build-config.js",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0",
    "migrate:firestore": "node scripts/migrate-courses-to-firestore.js"
  },
  "devDependencies": {
    "vite": "^8.0.12"
  },
  "dependencies": {
    "firebase-admin": "^13.10.0",
    "nodemailer": "^8.0.7"
  }
}
